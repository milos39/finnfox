@model IEnumerable<finnfox.TipRacunovodstvenePromene>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NazivTipa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PozitivnostTipa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NazivTipa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PozitivnostTipa)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipPromeneId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipPromeneId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipPromeneId })
        </td>
    </tr>
}

</table>
